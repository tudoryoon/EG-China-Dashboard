# EG China & HK & Taiwan Dashboard — fixed project rules

## User decisions (2026-09-07)

- This repository is the independent China & Hong Kong & Taiwan dashboard.
- **UI/UX must remain identical to EG Dashboard**: https://tudoryoon.github.io/EG_Dashboard/#/taiwan/overview . This is a fixed default, not an invitation to redesign.
- Preserve the original typography, colors, spacing, tabs, cards, tables, filters, charts, responsive behavior, and interactions. Reuse the original CSS and renderers. Changes to this rule require an explicit user request.
- First migration: existing Hong Kong and China RS and Trend Score, their data and update workflow, plus the existing Taiwan revenue overview. Taiwan equity RS is not in the source and has not been invented here.
- Keep the reference EG_Dashboard repository unchanged when working on this repository.
- User correction on 2026-09-07: remove the large `China & HK & Taiwan` parent tab. The main tabs are **RS → 추세스코어 → Taiwan**. RS and 추세스코어 each contain **중국 → 홍콩** subtabs; Taiwan has no country subtabs. Keep the original visual components and styling with this explicitly requested navigation change.
- Default entry is **RS / 중국** (`#/rs/china`). Never use Taiwan as the default route or the parent route of Chinese/Hong Kong screening. Preserve the selected market when switching between RS and 추세스코어.

## Calculations and data

- Use `scripts/update_asia_screening.py`, which calls the original `update_market_rs.py` and `update_market_trend_score.py` engines. Do not independently approximate the formulas.
- Rank Hong Kong and China equities independently in local currency. ETFs are tracked but excluded from equity RS rankings. Shared market-cap filters use USD; chart prices remain HKD/CNY.
- User-requested missing-score repair (2026-09-08): ETFs now receive supplementary RS percentiles against their regional equity population, each ETF independently, using the original engine/weights. They do not affect any equity or other ETF's rank. Collect all five ETF histories from Eastmoney, using forward-adjusted OHLC for charts/RS/trend so unit splits do not create false crashes, while retaining raw closes/volume. Preserve actual missing sessions and require ETF dates to match the regional benchmark before publishing. Disclose source and score basis in JSON. Add explicit actual close-to-close `returns.1d` without bridging missing market sessions.
- For 6082.HK and 0100.HK, keep the original trend engine's stricter 60-session available-history threshold while fewer than 200 observations exist. Other regional listings use the original engine's existing 20-observation provisional threshold requested in the 2026-09-10 completeness repair. Display the existing provisional score-basis field and return to standard mode once the latest 200-session window is complete. 3109.HK uses the standard score after verified source history repairs, not provisional scoring.
- User-requested STAR50 index addition (2026-09-09): collect `000688.SS` (STAR 50 Index) daily and include it in both China RS and Trend Score. Classify it as `Index`, never as an equity or ETF. Its RS is an independent percentile against the China equity population using the original RS engine, so it does not alter any equity or ETF score. Trend uses the unchanged original engine and CSI800 benchmark. Accept only the latest completed XSHG session from Tencent with a separately validated Eastmoney fallback.
- User-requested completeness repair (2026-09-10): every ordinary Hong Kong and China equity must use the latest completed exchange session before publication. Tencent raw OHLC plus its qfq adjusted close is primary; Yahoo Finance is a freshness-checked fallback. Never let a current benchmark date conceal lagging equity rows. Listings and securities with gaps in the latest 200-session window use the original trend engine's available-history long-term average mode with an explicit provisional basis; use its existing 20-observation floor except for the 60-session thresholds retained for 6082.HK and 0100.HK.
- User watchlist additions (2026-09-08): 3033.HK, 6083.HK, 588200.SS (C1), 3109.HK, 0100.HK (100 HK), 562500.SS (C1), 159819.SZ (C2). Keep 6083.HK (World-Link Logistics) separate from 6082.HK (Biren). Support HK/C1/C2 search notation in both screening views. All filters must retain unscored ETFs/new listings and unknown market caps; explicit numeric filters must exclude missing values rather than treating them as zero.
- Hong Kong uses the explicitly disclosed HSI fallback for unavailable HSCI history. China uses CSI800. Preserve missing observations and holiday handling.
- Regional data stays in `data/asia-*.json`; do not introduce the US universe.
- Canonical routes are `#/rs/china`, `#/rs/hong-kong`, `#/trend-score/china`, `#/trend-score/hong-kong`, and `#/taiwan`. Old `#/taiwan/...` screening links are compatibility redirects only, never newly generated navigation URLs.
- Run `python scripts/validate_asia_screening.py`, `python scripts/validate_migration.py`, and `node --check dashboard.js` before publishing. Check all five views in a browser after UI changes.
- `docs/migration-source.json` records the source commit and unchanged baseline hashes. Update provenance deliberately when importing future upstream changes; daily data refreshes may naturally change snapshot hashes.

## Delivery

- Daily data refresh is scheduled through three Actions at **21:03, 21:10, 21:17 KST**, every day. All three call the same workflow and share a queued concurrency lock; read `main` after acquiring the lock.
- Refresh Hong Kong/China screening and Taiwan revenue together. Publish only after all collectors and validators succeed. A complete checkpoint for the same 21:03 KST refresh cycle, matching data and pipeline hashes, suppresses later collection, commits, pushes, and deployments even when GitHub delays runs past midnight. Failed or stale refreshes must leave later retries enabled. A morning manual refresh never suppresses the next 21:03 cycle.
- HSCI API rows must be filtered to explicit `isDummy=N` before validating the official constituent count; residual blank/dummy rows can remain after index reviews. Never bypass missing-member or duplicate-symbol checks.
- Source reliability repair (2026-09-09): Tencent is the primary source for all five ETFs and HSI/CSI800 histories, with Eastmoney ETF/CSI800 and Yahoo HSI fallbacks. Require the exchange calendar's latest completed session before accepting any provider, including HTTP-success responses. Retain entire adjusted histories from one provider; do not splice incompatible price adjustments or fill internal gaps. Tencent's shorter leading adjusted-history window may be trimmed explicitly. Log the selected provider/date and failed provider reasons. The manual `Verify Full Data Refresh` Action must perform real collection/validation with `--force`, has read-only permissions and never pushes/deploys. Scheduled jobs keep their normal checkpoint skip behavior.
- Scheduled publication validation must inspect every regional row's `asOfDate` and last history observation, not only the benchmark-level `updatedAt`. A lagging ordinary equity invalidates the checkpoint so the 21:10 and 21:17 jobs retry instead of accepting a partial-date snapshot.
- Do not replace this with three unguarded push jobs. Run `python -m unittest discover -s tests -v` for schedule/refresh changes.

- Carry over the source project's authorized workflow: validate, create a scoped commit, and normally push to `origin/main` without asking again. Respect explicit no-push or review-only requests. Never force-push.
- Deployment uses this repository's GitHub Pages. Only the regional refresh workflow is migrated; do not enable unrelated US-market jobs.
